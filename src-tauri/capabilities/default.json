{
  "identifier": "default",
  "description": "The launcher window may drive its own window chrome (minimize / close). The game window gets no capability — it runs downloaded content as sandboxed web content with no Tauri bridge.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-start-dragging",
    "updater:default"
  ]
}
